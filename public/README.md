# Carpeta pública (Vite sirve aquí en la raíz /)

- logopcs.ico   → Icono instalador Windows (NSIS) + ventana Electron en Win
- logopcs.png   → Icono instalador Linux (AppImage / .deb) + fallback
- favicon.svg   → Favicon browser (opcional, placeholder incluido)

Si reemplazas estos archivos por tus logos reales, automáticamente se usan al build.
